To Use:

1. Open command line window type command: mvn spring-boot:run
2. Once application has started, using some API testing software (postman) send a call to http://localhost:8090/api/v1/dictionary/{enter_your_word_here}/spelling
3. If a 200 repsonse comes back your word is spelled correctly.